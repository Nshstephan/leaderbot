[project]
name = "leaderboard"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "python-telegram-bot[job-queue]==22.7",
    "gspread==6.2.1",
    "google-auth==2.35.0",
]

[tool.uv]
package = false
